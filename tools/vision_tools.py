def find_element(description):

    screenshot = screen.screenshot()

    # Send screenshot to vision model

    # Get coordinates

    return {
        "success": True,
        "element": description,
        "x": x,
        "y": y
    }